{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Debug app (python -m app)",
      "type": "python",
      "request": "launch",
      "module": "app",
      "justMyCode": false,
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "python": "${workspaceFolder}/.venv/bin/python",
      "console": "integratedTerminal"
    }
  ]
}
